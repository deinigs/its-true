describe("It's True", () => {
    describe("itsTrue function", () => {
        it("should works with booleans.");
        it("should works with numbers (1 -> true, All else -> false)");
        it("should works with arrays, objects, functions. All -> false");
        it("should works with strings. 'true' (different cases) -> true");
        it("should works with undefined, null, NaN. All -> false");
    });
});